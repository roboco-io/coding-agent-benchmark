# EXP-028 결과 보고: Claude Code × Sonnet 5 네이티브 랄프 루프 완주 검증 (EN·KO 각 n=3)

- 실험일: 2026-09-24 (10:16–11:42 KST, 단일 오케스트레이터 순차 6 run, EN/KO 교차 순서)
- 가설: [M-21](../../hypotheses/catalog.md) — Claude Code 네이티브 하네스에서 Sonnet 5(`claude-sonnet-5`, thinking 기본값)는 EN·KO 정본 랄프 루프로 RealWorld 백엔드(Hurl 13/13·154/154)를 상한 10 iteration·4h 안에 무개입 완주할 수 있다 (EN·KO 각 n=3, 완주율 판정·과금 배제).
- **판정: 검증** — EN 3/3·KO 3/3 완주 (게이트 pass + 독립 재검증 각 2회 13/13·154/154 일치, 개입 0, 응답 model 필드 전수 `claude-sonnet-5`). 5 run은 iteration 1, en-1은 에이전트가 작업을 3개 iteration으로 나눠 iteration 3에 완주했다. 세션 11.2–16.8분·output 57.6–73.7K로 같은 하네스의 Opus 5.5(EXP-026)·Fable 5.1(EXP-023)보다 길고 많은 대역에 놓인다. 관측값이며 우열 확정이 아니다.

> 이 실험은 `ralph-model-benchmark` 스킬의 `claude-native` 하네스를 처음 적용한 실험이다.

## 사전 기준 대조

| 주 지표·사전 기준 | 관측값·불확실성 | 충족 여부 | 판정 범위 |
|------------------|----------------|-----------|----------|
| EN·KO 각 3/3 예산 내(10 iter·4h) 게이트 + 재검증 2회 일치 완주 → 검증 | en 3/3(iter 3·1·1), ko 3/3(전부 iter 1). 조건당 n=3이라 완주율 구간은 넓다(3/3 관측, 95% 신뢰구간 하한 약 0.29) | 충족 | 완주 가능성 |
| run 단위 보류 조건(서비스 장애·하네스 결함) | 해당 없음 — 5xx·레이트 리밋·게이트 오검·채점 행 대기 0건 | — | — |

## run별 결과

| run | 조건 | 예산 내 합격 | 채점(게이트 / 재검증×2) | 세션 시간 | 커밋 | output | 토큰 대리지표 | 개입·장애 |
|-----|------|-------------|------------------------|-----------|------|--------|--------------|-----------|
| en-1 | EN | iter 3 | 13/154 · 13/154 ×2 | 15분 20초 (3:21 + 3:29 + 8:30) | 5 | 73.7K | 270.8K | 없음 |
| ko-1 | KO | iter 1 | 13/154 · 13/154 ×2 | 14분 10초 | 5 | 64.7K | 213.7K | 없음 |
| en-2 | EN | iter 1 | 13/154 · 13/154 ×2 | 14분 12초 | 2 | 57.6K (서브에이전트 9.7K 포함) | 223.2K | 없음 |
| ko-2 | KO | iter 1 | 13/154 · 13/154 ×2 | 16분 45초 | 4 | 72.1K | 226.3K | 없음 |
| en-3 | EN | iter 1 | 13/154 · 13/154 ×2 | 12분 57초 | 4 | 59.2K | 193.2K | 없음 |
| ko-3 | KO | iter 1 | 13/154 · 13/154 ×2 | 11분 14초 | 3 | 66.4K (서브에이전트 7.2K 포함) | 222.4K | 없음 |

- 세션 시간은 로그의 iteration start–end(`claude -p` 실행 구간) 합이다. 게이트 채점 약 10초와 iteration 사이 간격은 제외했다(en-1 wall-clock은 15분 43초).
- 토큰 대리지표는 `input + output + cache_creation`(서브에이전트 세션 포함)이며 청구 비용이 아니다(구독 OAuth 실행, 실제 청구 미측정).
- 실패·시간 초과·제외·재실행 없음. 과제 1종의 반복이다.

## en-1의 다중 iteration

en-1은 게이트 반려(rejected) 없이 iteration 3에 완주했다. 에이전트가 첫 iteration에서 스캐폴딩만 하고 "다음 iteration에서 인증·CRUD 구현"이라고 적은 뒤 종료했고, iteration 2에서 공식 Hurl 테스트 vendoring과 실행 스크립트만 만들고 종료했다. 두 iteration 모두 완료 선언이 없었고 채점은 0/13(각 파일 첫 요청 실패로 13건만 실행)이었다. iteration 3에서 API 전체를 구현하고 13/13을 확인한 뒤 완료를 선언했다. 즉 실패 후 복구가 아니라 에이전트가 스스로 작업을 여러 세션으로 나눈 사례이며, 랄프 루프가 이 분할 실행을 받아 완주로 이어졌다. 이번 실험에서 루프의 실패 복구 기여는 관측되지 않았다.

## 조건별 비교 (관측값 병치, 우위 판정 없음)

| 지표 | Sonnet 5 EN | Sonnet 5 KO | Opus 5.5 (EXP-026, 09-23) | Fable 5.1 EN (EXP-023) |
|------|------------|------------|---------------------------|------------------------|
| 합격 / run | 3/3 | 3/3 | 6/6 | 3/3 |
| 완주 iteration | 3 / 1 / 1 | 1 / 1 / 1 | 전부 1 | 전부 1 |
| 세션 시간(분) | 15.3 / 14.2 / 13.0 | 14.2 / 16.8 / 11.2 | 4.0–8.4 | 6.2–7.8 |
| output(K) | 73.7 / 57.6 / 59.2 | 64.7 / 72.1 / 66.4 | 18.6–28.2 | 28.7–35.8 |
| git 커밋 | 5 / 2 / 4 | 5 / 4 / 3 | 1–6 | 2–4 |
| API 호출(고유 message.id, 메인) | 104 / 88 / 105 | 96 / 140 / 82 | 22–41 | 26–34 |

- Sonnet 5의 세션 시간·output·API 호출 수는 6 run 모두 Opus 5.5·Fable 5.1의 관측 범위 밖(위쪽)이다. 날짜·Claude Code 버전이 다르므로(EXP-026은 2026-09-23) 모델 단독 차이로 단정하지 않는다.
- EN과 KO 사이에 일관된 방향의 차이는 보이지 않았다(n=3).

## 모델 유지·노출 설정

- 6 run 모든 고유 메시지(서브에이전트 포함)의 `model` 필드가 `claude-sonnet-5`. fallback 없음.
- 비격리 조건(EXP-023/026과 동일): 사용자 글로벌 CLAUDE.md·플러그인 스킬·MCP가 노출됐다. 실제 호출은 Bash·Read·Write·Edit와 Agent(en-2 general-purpose 1회, ko-3 Explore 1회 — 둘 다 Hurl 테스트 요약용, 서브에이전트는 Read만 사용)뿐이며 MCP·Skill 호출은 0건이다.
- 글로벌 CLAUDE.md의 "한국어로 응답" 규칙 때문에 EN 프롬프트 run의 최종 요약도 대부분 한국어로 작성됐다(en-1 iteration 1·3은 영어). 코드·커밋 메시지는 영어였다.
- 호스트의 3000 포트를 다른 로컬 프로세스(VS Code helper)가 점유하고 있어 en-1은 기본 포트를 4000으로 바꿨다. measure v4는 리포 소속 포트를 자동 탐지하므로 채점에 영향 없음.
- run별 프로젝트 디렉터리의 auto memory(`memory/`)는 6 run 모두 비어 있어 run 간 기억 전달은 없었다.

## usage·집계 검증

- 원자료: `~/.claude/projects/-Users-dohyunjung-ralph-exp028-app-<run>/` → `sessions-<run>/` 복사본. 메인 세션 jsonl은 en-1 3개(iteration당 1개), 나머지 1개. 서브에이전트 jsonl은 en-2·ko-3에 1개씩.
- 집계: `runs/usage028.py`(message.id dedup, 메인·서브에이전트 분리, 모델·도구 호출 기록). 메인 세션 값은 `scripts/aggregate_tokens.py` v2(top-level JSONL만 집계) 결과와 일치한다(en-1 output 73,701 등). v2는 서브에이전트 jsonl을 집계하지 않으므로 서브에이전트 값은 `usage028.py`로 따로 더했다.
- 재현 자료: `bench.env`, 스크립트 사본, `phase0.md`/`phase0.log`, `metrics-*.csv`, `recheck.csv`, `sessions.csv`, `usage.csv`, 로그(gz).

## 한계와 교란 변수

- 과제 1개·조건당 3 run. 3/3은 이 조건의 관측값이며 일반적인 성공률을 확정하지 않는다.
- 비격리 실행이라 사용자 글로벌 설정이 행동(응답 언어, 서브에이전트 사용 가능성)에 영향을 줬다. EXP-023/026과 같은 조건이지만, 격리 조건 결과와 직접 비교하지 않는다.
- Anthropic 모델 목록 API는 키 미설정으로 조회하지 못했다. 모델 ID는 스모크 응답과 세션의 `model` 필드로 확인했다.

## 결론 및 후속 실험

- 결론: Sonnet 5는 Claude Code 네이티브 하네스에서 EN·KO 정본 모두 이 과제를 완주했다(각 n=3). 같은 하네스의 Opus 5.5·Fable 5.1보다 세션이 길고 output이 많았으며, 1 run은 작업을 3개 iteration으로 나눴다.
- 후속: 동시기 Opus 5.5 재측정을 포함한 교차 비교, 격리(`claude-direct` 형 `CLAUDE_CONFIG_DIR`) 조건 재측정.
- 동기화: catalog M-21, ROADMAP, 4개 README(`update_readme_results.py`), 대시보드 반영.
